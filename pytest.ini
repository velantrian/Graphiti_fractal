[pytest]
asyncio_mode = strict
filterwarnings =
    error::RuntimeWarning
markers =
    integration: requires an explicitly configured isolated Neo4j/Graphiti environment
    external: requires explicit provider credentials and execution gate
